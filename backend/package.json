{
  "name": "pat-backend",
  "version": "1.0.0",
  "private": true,
  "description": "PAT (Personal Access Token) management API and Lambda Authorizer",
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1106.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "aws-jwt-verify": "^4.0.1",
    "hono": "^4.12.33"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.5.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2"
  }
}
